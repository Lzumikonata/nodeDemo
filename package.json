{
  "name": "node",
  "version": "1.0.0",
  "description": "a node demo",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start server.js --name process-api ecosystem.config.js"
  },
  "author": "wuke",
  "license": "ISC",
  "devDependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2"
  },
  "dependencies": {
    "mongoose": "^5.1.0"
  }
}
